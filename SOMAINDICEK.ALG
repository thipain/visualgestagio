Algoritmo "SomaIndiceK"
Var

   indice, soma, k: Inteiro

Inicio

   indice <- 13
   soma <- 0
   k <- 0

   enquanto k < indice Faca
      k <- k + 1
      soma <- soma + k
   FimEnquanto

   Escreva("O valor da variável soma é: ", soma)
FimAlgoritmo